apiVersion: v1
kind: Service
metadata:
  name: {{ template "WhackAMole.fullname" . }}
  labels:
    app: {{ template "WhackAMole.name" . }}
    chart: {{ template "WhackAMole.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "WhackAMole.name" . }}
    release: {{ .Release.Name }}
